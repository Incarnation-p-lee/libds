.SUFFIXES:
.SUFFIXES: .o .a .so .h .c .s .S

SHELL        =/bin/sh
AR           =ar
CC           =gcc
LD           =ld
#CC           =clang-3.5

AFLAG        =-rc
LFLAG        =-shared
CFLAG        =-Wall -Werror -c
CFLAG        +=$(cc_config)
LFLAG        +=$(ld_config)

ELF_TARGET   =ds.elf
OBJ_TARGET   =libds.a
DYN_TARGET   =libds.so
EXTLIB       =-lm

